# mongo-service.yml
# Service untuk memberikan akses internal ke MongoDB di dalam cluster.
apiVersion: v1
kind: Service
metadata:
  name: mongo-service
spec:
  type: ClusterIP
  selector:
    app: mongo
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
